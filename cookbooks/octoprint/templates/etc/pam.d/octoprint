auth requisite pam_succeed_if.so uid >= 1000
auth sufficient pam_exec.so expose_authtok /usr/bin/sudo -nu root <%= @unix_chkpwd_wrapper_path %>
@include common-account
@include common-session-noninteractive